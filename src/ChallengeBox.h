#pragma once
#include <SFML/Graphics.hpp>
#include <string>
//#include "Mechanic.h"
/*
using namespace std;

class ChallengeBox{
    public:    
        ChallengeBox(sf::RenderWindow* win, wstring chl_str);
        ~ChallengeBox();
        void Calculate();
        void DrawText();

    private:
        inline int GetCharLength(char &letter, sf::Font &font, int font_size);
        wstring challenge_str;
        sf::RenderWindow* window = NULL;
        sf::Text txt;
}; 
*/ 